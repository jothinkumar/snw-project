---
import HoverWindow from "./HoverWindow.astro"
const {data,name,options}=Astro.props
---

<div id={`navbar-${data}-parent`}>
    <div class="navbar-opt" id={`navbar-${data}`}>
    {name}
    </div>
<HoverWindow data={data} options={options}>
    <slot></slot>
</HoverWindow>
</div>